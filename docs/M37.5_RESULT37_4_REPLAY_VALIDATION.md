# M37.5 Replay Validation on the M37.4 Failure Captures

Input: the 14 saved exact RGB-D debug bundles in `result_37.4.tar.gz`.

## Safety outcomes

- The two initially reported M36 axis failures are rejected by the new
  depth-plane/ellipse conflict gate, and the hybrid selector continues to a
  different target.
- M37 uses organized surface normals and neighboring-instance exclusion.
- Several contaminated fast fits are rejected before selection by normal-radial
  consistency, bootstrap stability, or axis-seed disagreement.
- The last capture (`1785853985183`) produces no target instead of publishing
  an uncertain axis.
- No replay uses the legacy complete global accurate fallback.

## Visual outcome

The validation montage draws the M37.5 far-to-near axis in magenta/red. Across
the reported contact cases, the selected axis follows the visible short-cylinder
length rather than the previous low-residual but incorrect diagonal solution.

This is an offline replay on the local validation machine. RK3576 latency and
field accuracy must still be confirmed after deployment.
