# M38.6.1 Orbbec 336L RGB 曝光设置

在 M38.6 基础上新增：

- Orbbec SDK Bridge 支持 RGB 自动/手动曝光；默认自动曝光。
- 环境变量：`VISIONOPS_ORBBEC336L_COLOR_AUTO_EXPOSURE=true`、`VISIONOPS_ORBBEC336L_COLOR_EXPOSURE=50`。
- 每次启动 Pipeline、USB 断线重连后重新应用曝光。
- `/health` 与 `/stream/status` 返回曝光配置、设备读回值、支持状态和错误。
- Web“设置 → 相机设置”新增曝光模式与手动曝光值输入；HP60C 下自动隐藏。
- 保存配置会写入 `orbbec336l_bridge.env`，重启 Bridge，并校验设备是否接受设置。

部署后执行：

```bash
cd /opt/visionops_v3/edge/camera_bridge/orbbec336l_bridge
sudo bash install_orbbec336l_bridge_service.sh
sudo systemctl restart visionops-orbbec336l-bridge.service

curl -s http://127.0.0.1:18182/stream/status | python3 -m json.tool
```
