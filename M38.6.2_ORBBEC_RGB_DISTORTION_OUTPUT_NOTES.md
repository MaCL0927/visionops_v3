# M38.6.2 Orbbec 336L RGB 镜头畸变参数输出

基于 M38.6.1，仅扩展 336L SDK Bridge 的 `/stream/camera_info`。

## 修改内容

- 调用当前 Color `VideoStreamProfile::getDistortion()` 读取 Orbbec SDK 原始 RGB 畸变参数。
- 输出 `model/model_name`、`k1~k6`、`p1/p2`。
- 对 `NONE`、`BROWN_CONRADY`、`BROWN_CONRADY_K6` 额外输出可直接构造成 OpenCV `distCoeffs` 的数组：
  `[k1, k2, p1, p2, k3, k4, k5, k6]`。
- 对 Modified/Inverse Brown-Conrady 或 Kannala-Brandt4，不伪装成 OpenCV Brown-Conrady；`opencv_compatible=false` 且 `opencv_dist_coeffs=null`。
- 不改变 RGB/Depth 采集、D2C、曝光控制、共享内存、重连或 Foam Ring M38.6 抓取逻辑。

## 部署

```bash
cd /opt/visionops_v3/edge/camera_bridge/orbbec336l_bridge
sudo bash install_orbbec336l_bridge_service.sh
sudo systemctl restart visionops-orbbec336l-bridge.service

curl -s http://127.0.0.1:18182/stream/camera_info | python3 -m json.tool
```

正式开始 M39 Eye-to-Hand 标定前，记录该接口的完整输出，并确认 `model_name` 与 `opencv_compatible`。
