# carton_partition_check

负责检测 5×8 隔板小方格、完成网格排序、模板误差分析和结构 OK/NG 判断。

- 算法：`algorithm.py`
- 标定模板：`assets/partition_template.json`
- 参数：`../../config/line.yaml` 中的 `partition.algorithm`
- 坐标映射：`../../config/line.yaml` 中的 `coordinates`
